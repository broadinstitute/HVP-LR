# hvp-qc — read/report QC tools carried by the monolith (fastqc + multiqc).
name: base
channels: [conda-forge, bioconda]
dependencies:
  - fastqc=0.12.1
  - multiqc=1.35
  - setuptools>=78.1.1     # CVE-2025-47273
  - procps-ng
