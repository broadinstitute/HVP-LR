# kraken2 — taxonomic classifier. Serves HifiKraken2. (bracken dropped: no WDL
# task invokes it.) Pin verbatim from hvp-monolith base env.
name: base
channels: [conda-forge, bioconda]
dependencies:
  - kraken2=2.17.1
  - procps-ng
