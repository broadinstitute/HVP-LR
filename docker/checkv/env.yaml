# checkv — viral genome completeness/contamination QC. Bioconda pulls its
# diamond + prodigal-gv + hmmer deps automatically. Serves CheckV.
name: base
channels: [conda-forge, bioconda]
dependencies:
  - setuptools>=78.1.1   # CVE-2025-47273 (base env ships 70.3.0)
  - checkv=1.1.0
  - procps-ng
