# vcontact2 — viral genome clustering. Sidecar py3.8 env. Pins verbatim from
# hvp-monolith env-vcontact2.yaml (numpy pinned <1.24 for np.warnings usage).
name: base
channels: [conda-forge, bioconda]
dependencies:
  - python=3.8.20
  - vcontact2=0.11.3
  - numpy=1.23.5
  - diamond=2.1.24
  - mcl=22.282
  - clusterone=1.0
  - procps-ng
