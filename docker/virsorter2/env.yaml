# virsorter2 — viral sequence identification. Own python=3.10 env (virsorter
# <=2.2.4 requires python <=3.10). vibrant dropped: no WDL task invokes it.
# Serves VirSorter2.
name: base
channels: [conda-forge, bioconda]
dependencies:
  - python=3.10.20
  - virsorter=2.2.4
  - procps-ng
