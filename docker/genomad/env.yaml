# genomad — viral/plasmid detection. Serves Genomad. Pin verbatim from base env.
name: base
channels: [conda-forge, bioconda]
dependencies:
  - genomad=1.12.0
  - pigz=2.8
  - zstd=1.5.7
  - procps-ng
