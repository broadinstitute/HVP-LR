# vibrant — VIBRANT viral identification/annotation. Requires python<=3.10.
# Pin verbatim from hvp-monolith env-viral.yaml (virsorter dropped — it has its
# own image). VIBRANT database is downloaded at runtime via VIBRANT_setup.py.
name: base
channels: [conda-forge, bioconda]
dependencies:
  - python=3.10.20
  - vibrant=1.0.1
  - procps-ng
