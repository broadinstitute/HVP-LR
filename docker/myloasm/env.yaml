# myloasm — metagenomic assembler (+ seqkit for assembly stats). Serves Myloasm.
name: base
channels: [conda-forge, bioconda]
dependencies:
  - myloasm=0.5.1
  - seqkit=2.13.0
  - pigz=2.8
  - procps-ng
