# HVP-LR
## Dev environment setup:
Run this to create your dev environment:
```
python3 -mvenv venv && . venv/bin/activate && pip install -r dev-requirements.txt
```

Then install the pre-commit hooks for your clone:
```
pre-commit install
```

Your first commit will take a bit longer to run.  This is normal.
noop
